# Create-directory
create a directory if not exits with the same name


Bash shell script
editor: Vim linux

create a folder with a name the user will enter. If the folder already exists, an error message appears.
